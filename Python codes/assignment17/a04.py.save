## IMPORTS GO HERE

## END OF IMPORTS

#### YOUR CODE FOR get_grade() FUNCTION GOES HERE ####
def get_grade(total_marks):
    if total_marks in range(86,91):
        return  "A+"
    if total_marks in range(82,86):
        return "A"
    if total_marks in range(78,82):
        return "A-"
    if total_marks in range(74,78):
        return "B+"
    if total_marks in range(70,74):
        return "B"
    if total_marks in range(66,70):
        return "B-"
    if total_marks in range(62,66):
        return "C+"
    if total_marks in range(58,62):
        return "C"
    if total_marks in range(54,58):
        return "C-"
    if total_marks in range(50,54):
        return "D+"
    if total_marks in range(50,54):
        return "D"
    if total_marks in range(0,50):
        return "F"

#### End OF MARKER

#### YOUR CODE FOR calculate_sgpa() FUNCTION GOES HERE ####
def f(x):
    if  x == ('A+'):
        return 4
    if x == ('A'):
        return 4
    if x ==('A-'):
        return 4
    if x == ('B+'):
        return 3
    if x == ('B'):
        return 3
    if x == ('B-'):
        return 3
    if x == ('C+'):
        return 2
    if x == ('C'):
        return 2
    if x == ('C-'):
        return 2
    if x == ('D+'):
        return 1
    if x == ('D'):
        return 4
def calculate_sgpa(grade1,grade2,grade3): 
    points = 0
    number_of_subjects = 0
    if grade1 != 0:
        number_of_subjects = total_number_of_subjects + 1
        points = points + points(grade1)
    if grade2 != 0:
        number_of_subjects = number_of_subjects + 1
        points = tot + 4
    if grade3 != 0:
        total_number_of_subjects = total_number_of_subjects + 1
        total_marks = total_marks + 4
    if total_number_of_subjects !=0:
        return 0 
    SGPA = total_marks / total_number_of_subjects
    return SGPA


#### End OF MARKER




if __name__ == '__main__':
    print get_grade(50)
    print calculate_sgpa('A', 'B', 'nothing')
